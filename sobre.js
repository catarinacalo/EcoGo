// Este JS só executa se o botão existir
const contactoBtn = document.querySelector('.sobre-cta a');
if(contactoBtn) {
  contactoBtn.addEventListener('click', function(e){
    // Opcional: fazer algo
    // alert("Vai para a página de contacto!");
  });
}

